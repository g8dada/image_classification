import os

CONFIG = {
    "data_dir": "data/",
    "batch_size": 32,
    "epochs": 20,
    "learning_rate": 0.001,
    "num_classes": 6,
    "image_size": 150,
    "checkpoint_dir": "checkpoints/",
    "log_dir": "logs/",
    "classes": ['buildings', 'forest', 'glacier', 'mountain', 'sea', 'street']
}
